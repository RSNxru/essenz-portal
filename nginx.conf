# Portal Essenz SPA (raíz del dominio). Fallback a index.html para el
# routing del lado del cliente (React).
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
